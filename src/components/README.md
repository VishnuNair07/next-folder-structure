# /components

Shared, reusable UI components used across multiple modules.

### This folder contains:
- form/ → shared form input components
- buttons/ → reusable buttons
- layout/ → shared layout building blocks
- ui/ → generic UI widgets

## Rules
- No business logic
- Must be reusable
- Keep them clean and presentation-focused