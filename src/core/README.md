# /core

Infrastructure-level folder.

Contains:
- api-client → axios instance + interceptors
- config → env & app config
- hooks → global hooks
- utils → framework utilities

No module-specific or business logic belongs here.