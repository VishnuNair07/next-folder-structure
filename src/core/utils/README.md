# /utils
Framework / Infrastructure utilities
Small framework-level helper utilities.

Used by core architecture like API client, guards.

Examples:
- cn (className merge)
- date utilities
- safe JSON parsing

Must be pure functions without side effects.