# /api-client

Contains the global axios instance and HTTP utilities.

Files:
- axios-client.ts → axios instance with interceptors
- api-error.ts → normalized error class
- http-status.ts → HTTP status codes

Used by all services across modules.